# Kopiere diese Datei nach .env und fülle sie aus.
# Starten dann mit:  node --env-file=.env watcher.mjs

# --- Pflicht ---
UC_COOKIE="hier die Cookie-Zeile aus dem Browser einfügen"
UC_NTFY_TOPIC=uc-firma-9f3a2b7c

# --- Seiten ---
UC_DASHBOARD_URL=https://unicacity.eu/dashboard/unternehmen
# Nur setzen, wenn die Team-Leiste auf einer anderen Seite steht:
#UC_ONLINE_URL=

# --- Spieler (Reihenfolge egal) ---
UC_SPIELER=LottiMi,maaxxyyy,halo361,Lexae,777ELITE,jqshey

# --- Schwellwerte ---
UC_LAGER_SCHWELLE=500
UC_PERSONAL_SOLL=6
UC_ERINNERUNG_MIN=60
UC_AUSSCHUETTUNG_STD=12
UC_AUSSCHUETTUNG_SCHWELLE=1000

# --- Betrieb ---
UC_INTERVALL_MS=60000
UC_STATE_FILE=./uc-watcher-state.json
UC_DEBUG=0
